services:
  - type: web
    name: viper-web
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx tsx web-api.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /health
